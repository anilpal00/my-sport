package com.dailyinsuranceportal.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.dailyinsuranceportal.entities.UserPolicyClaim;

public interface UserPolicyClaimRepository extends JpaRepository<UserPolicyClaim, Long> {

//	@Query(value = "SELECT * FROM dailyinsuranceportal.user_policy_claim where purchase_dttm = :purchase_date and policy_id=:policyId and user_id = :uid", nativeQuery = true)
//	List<UserPolicyClaim> getAllClaimData(Policy policyId, @Param("purchase_dttm") Date purchase_date, @Param("uid") User uid);
}
