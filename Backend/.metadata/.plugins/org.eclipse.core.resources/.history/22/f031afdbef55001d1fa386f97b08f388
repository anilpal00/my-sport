package com.mysport.service;

import java.time.LocalDateTime;

public interface AppointmentService {
	
	void createNewAppointment(Long userId, LocalDateTime start);
	
	boolean isAvailable(int workId, int providerId, Long userId, LocalDateTime start);
}
